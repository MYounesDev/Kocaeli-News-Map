# ──────────────────────────────────────────────
# Requirements for Vercel Serverless Deployment
# ──────────────────────────────────────────────
# NOTE: sentence-transformers, scikit-learn, and torch are excluded
# because they far exceed Vercel's 250 MB function size limit.
# The scraper endpoints will return an error if invoked on Vercel.
# Run scraping from a local machine or a dedicated server instead.

# FastAPI & Server
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20

# MongoDB
motor==3.7.1
pymongo==4.12.1

# Data Validation & Config
pydantic==2.11.1
pydantic-settings==2.8.1
python-dotenv==1.1.0

# HTTP Client
httpx==0.28.1

# HTML Parsing
beautifulsoup4==4.13.3
lxml==5.3.1

# Date Parsing
python-dateutil==2.9.0.post0

# CORS
starlette==0.46.1
